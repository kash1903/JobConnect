spring.application.name=jobConnect

server.port = 8081




# ===============================
# = DataSource Config
# ===============================
# spring.datasource.url=jdbc:mysql://localhost:3306/jobConnect?useSSL=false&serverTimezone=UTC
# spring.datasource.username=root
# spring.datasource.password=
# spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver


# ===============================
# = DataSource Config (UPDATED FOR POSTGRES)
# ===============================

spring.datasource.url=${DB_URL}
spring.datasource.driver-class-name=org.postgresql.Driver
# spring.datasource.username=${DB_USERNAME}
# spring.datasource.password=${DB_PASSWORD}

# ===============================
# = JPA / Hibernate Config
# ===============================
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect




twilio.account.sid=${twilio.account.sid}
twilio.auth.token=${twilio.auth.token}
twilio.messaging.service.sid=${twilio.messaging.service.sid}
# twilio.phone.number=+17754068658



